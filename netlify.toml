[build]
  base = "frontend"
  publish = "build"
  command = "rm -f package-lock.json && npm install --legacy-peer-deps && CI=false npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
