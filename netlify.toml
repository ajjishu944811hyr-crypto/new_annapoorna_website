[build]
  base = "frontend"
  publish = "build"
  command = "rm -rf node_modules package-lock.json && npm install --force && CI=false DISABLE_ESLINT_PLUGIN=true npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
